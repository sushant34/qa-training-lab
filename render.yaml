services:
  - type: web
    name: qa-training-lab
    runtime: node
    buildCommand: npm install && cd server && npm install && cd ../client && npm install --include=dev && npx vite build
    startCommand: cd server && npm run seed && npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
